name: sampling_credit
channels:
  - conda-forge
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.11
  - numpy
  - pandas
  - tqdm
  - pip
  - pip:
    - vllm
    - torch
    - transformers
    - datasets
    - accelerate
    # dynasor for math evaluation (from hao-ai-lab, NOT PyPI)
    - git+https://github.com/hao-ai-lab/Dynasor.git
